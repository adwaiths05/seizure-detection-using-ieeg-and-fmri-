# Local Next.js (pnpm dev) talking to API in Docker on the host port:
NEXT_PUBLIC_API_URL=http://localhost:8000

# If the API runs on another host or port, set the full origin (no trailing slash):
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
